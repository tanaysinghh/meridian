// Removed — the v2 design language uses restrained blue radial washes via
// the `.hero-wash` class, not animated orbs. Kept as a no-op so any lingering
// import doesn't crash the build; delete once imports are cleaned up.
export default function GradientOrbs() { return null; }
